namespace sorec_gamma.modules.ModuleEvents.SignauxEvents.models
{
    public class VersionMTModel
    {
        public int NumVersion { get; set; }

        public override string ToString()
        {
            return "NumVersion: " + NumVersion;
        }
    }
}
